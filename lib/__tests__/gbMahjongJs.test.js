const assert = require('assert');
const gbMahjongJs = require('../index.js');

describe('gbMahjongJs', () => {
  it('has a test', () => {
    assert(false, 'gbMahjongJs should have a test');
  });
});
